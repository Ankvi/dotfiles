#!/bin/bash

if [ -z "$POLYBAR_CONFIG_PATH" ]; then
    echo "POLYBAR_CONFIG_PATH is not set. Please set it to the path of your polybar config folder."
    exit 1
fi

. $POLYBAR_CONFIG_PATH/launch.sh --kvist